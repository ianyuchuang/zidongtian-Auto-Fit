# 開發 / 工具用套件
#   pip install -r tools\requirements-tools.txt

# 測試（2_推至GitHub.bat 推之前會先跑）
pytest>=8.0

# 照片處理：experiments/*/run.py 讀圖與縮圖，測試也用到
pillow>=10.0

# Google 雲端硬碟備份（1_備份至Google雲端.bat）
google-api-python-client>=2.100
google-auth>=2.23
google-auth-oauthlib>=1.1
google-auth-httplib2>=0.1
