// 預設提示詞（白板欄位對應）。放獨立檔避免 index.js ↔ api.js 循環引用。
// 內容依 2026-09-03 對「尺寸11F」12 張的實測調整：模型最常犯的錯是把「標準值」格裡的
// 位置那一行當成尺寸、或把標準值抄進實際值，所以這裡把欄位對應講死。見 docs/辨識實測-尺寸11F.md。
export const DEFAULT_PROMPT = [
  '白板是一張表格，通常有「查驗項目」「查驗位置」「標準值」「實際值」等欄位。',
  '內容說明＝查驗項目，後面接查驗位置（例：輕隔間 1107梯廳）。',
  '設計＝「標準值」那一格裡的尺寸；實際＝「實際值」那一格裡的尺寸。',
  '注意：標準值那一格常常同時寫了「位置…」和「尺寸…」兩行——位置那一行屬於內容說明，只有尺寸那一行才是設計。',
  '兩格的尺寸通常長得很像（只差一兩個數字），不要因為像就把同一組數字填進兩欄，也不要把標準值抄成實際值；哪一格看不清楚就那一格留空。',
].join('');
